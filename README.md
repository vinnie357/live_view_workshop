# Phoenix LiveView Workshop

To start let's run:

```shell
make setup
```

That will install dependencies and compile the code.

Then run:

```shell
make server
```

Now you can visit [`localhost:4000`](http://localhost:4000) from your browser.

<img width="1029" alt="image" src="https://user-images.githubusercontent.com/1071893/172947158-2e855d01-fb5a-448c-9ebf-6ab4624f88f6.png">
